---

- hosts: cbit2
  vars_files:
    - vars.yml

  roles:

  - { role: system-setup, become: yes }
  - { role: ANXS.postgresql, become: yes }
  # TODO: Use this official role instead: https://github.com/elastic/ansible-elasticsearch
  - { role: geerlingguy.elasticsearch, become: yes, java_packages: [java-1.8.0-openjdk] }
  - { role: finish-setup, become: yes }

  - { role: backend, become: yes }